language: cpp
compiler:
  - gcc
  - clang
# Change this to your needs
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libboost-all-dev

script:
  - mkdir -p build 
  - cd build 
  - cmake ..
  - make
